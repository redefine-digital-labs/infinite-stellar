# Infinite Stellar Current Phase

- **Status:** In progress. The full multiplayer/mainnet objective is not complete.
- **Phase:** Real ranked proof preparation is verified through SDK witnesses, Circom and browser Workers; unsigned preparation is implemented, but the ranked map signing/finality consumer remains unfinished.
- **Worktree / branch:** `/Users/naoer/Documents/Claude/Projects/infinite-stellar`, `codex/dark-forest-parity`.
- **Authorization:** GitHub/Vercel delivery and production-valid mainnet engineering are authorized. GPL and MIT reuse are allowed with attribution and applicable source obligations. This does not waive ceremony, audits, custody, signer approvals or rights/operations gates.
- **Owner direction:** Preserve DF's mature gameplay, planet visuals, zoom/detail hierarchy, controls and entry flow. Concentrate innovation on Soul integration/history/season continuity and operating cadence. Stable product facts are in `PROJECT_MEMORY.md`.
- **Mainnet boundary:** No game transaction was signed or submitted. Production verifiers remain false. Ceremony, independent audits, approved multisig/custody, indexer/sponsor/monitoring, two-wallet soak, action wiring and operational clearance are unfinished. The full objective remains active.
- **Delivered baseline:** Continuous cancellable private exploration, exact persisted empty chunks, unified local command preview/dispatch, real-time growth/arrivals, cursor-anchored camera, map backups, original MIT normal-planet shader and zoom-dependent detail. Existing local gameplay is not evidence of live ranked play.
- **Previous deployed entry:** Source `e24662f8ec79d4e334d00cf0d7bf1c70a175c9bf`, deployment `dpl_AnUCr63FyzusegXHg58wAi8GnPcz`, production alias `https://infinite-stellar.vercel.app`. Local entry removed simulated approval ceremony; Home navigation/Continue preserves the encrypted save. Evidence: `ops/deployments/vercel-production-2026-09-05-entry-resume.json`.
- **Natural home implementation:** Soul selection starts real cancellable Worker mining from a randomly sampled public Round-5 home region. Exact MiMC/Perlin, rarity, level-0 Regular type and origin-centered radius are independently checked before creating the Home. Home stats/biome derive naturally; initial energy is 50,000. Completed search coverage and discovered locations persist; pause/reload resumes the same Seat, and another-region search retains completed knowledge.
- **No runtime fixture geography:** Removed the FNV candidate, fixed Home, hidden 48-coordinate bootstrap and forced Spacetime Rip from the player runtime. Controlled survey/rift data now lives only in `packages/game-sdk/test/strategy-fixture.ts`. New game creation has exactly one verified Home; only genuinely searched locations can add neighbors. Home search does not generate a fake proof digest.
- **Owner's new reset policy:** Each production build gets a fresh local-playtest storage namespace. Previous-build saves and old address-only sessions are not imported. Same-build navigation/refresh still resume progress. Wallets, canonical Soul, ranked vaults and chain state are untouched; old encrypted demo records are left inactive rather than broadly deleting storage.
- **Verification:** Node 24.20.0 full `npm run validate:web` passes 302 tests (136 web, 128 SDK, 38 prover), typecheck, lint and build. Tests cover exact home validation, no hidden bootstrap, completed footprint validation, actual progress, cancellation/late results, saved search/reload, fresh-build isolation, ignored older saves and growing energy during navigation. No timeout threshold was changed; test-only fixed survey geometry is computed once and cloned.
- **Browser evidence:** Fresh real Worker searches produced IS-BA7DE and IS-5BF92 in independent origins, each with 16,384 searched units². A third search paused before its first completed batch and resumed to IS-33073 with 4,096 units². BA7DE exploration added four natural neighbors; refresh retained all five Planets and 67,584 units² with continuing growth. No runtime error; only existing development Lit warnings. Testing pages were returned home with saves preserved.
- **Remaining fidelity:** Original special-facility renderer is not integrated; universe geography still uses the explicitly pinned public Round-5 keys and local radius, not a deployed ranked manifest. Soul candidates/Seat enrollment remain demo fixtures. Real proof/finality, special-action adapters and partially known Voyage feedback are unfinished.
- **Verified proof pipeline:** `proveRankedAction` snapshots the requested fleet/deadline, prepares the private witness, invokes local proving, re-reads the context and rejects changed nonce, ownership, existence, expiry, deployment or gates. `prepareRankedActionWithWorker` pins the action-specific production selection and disposes the isolated Worker. No private witness is returned or journaled, and neither helper signs or submits.
- **Worker fixes:** Cancel/replacement now terminates computation immediately and rejects late messages. Cached artifacts require identical complete selection, not just a matching digest; development-to-production cache reuse rejects. In-progress proving keeps its exact artifact snapshot.
- **Real proof evidence:** All three actual SDK witnesses pass development WASM/zkeys, local Groth16 verification and unsigned Sui transaction construction. Coordinate/statement tampering and production selection of development artifacts reject. The opt-in suite `npm run circuits:test:ranked` passes 3 tests without silent skipping. Full validation passes 330 standard tests (149 web, 143 SDK, 38 prover), types/lint/build.
- **Production CSP gap fixed:** A real browser reproduced `WebAssembly.compile` rejection under the previous response policy. Added only `wasm-unsafe-eval` and `worker-src 'self' blob:`; ordinary JS eval, inline scripts, connection origins and other security restrictions are unchanged. With the exact revised CSP on local Worker responses, home/move/move-new pass at 717/756/743 ms including local artifact loading (single-device observations, not p95). Cancelling a browser preflight stops the run. Development harness, public test fixtures and disposable keys are absent from the production build.
- **Next executable action:** Connect the ranked map to fresh `readPlayerSeatBundle` and selected-location projection/clock reads on each side of `prepareRankedActionWithWorker`; then wire explicit signature, public-only pending journal, finality/recovery and map refresh using existing transaction settlement checks. Never expose development pins or weaken release gates to make this route playable. Complete special actions and confirmed season operations within the unchanged full objective.
- **Latest release:** Source `f66c1f35c72ba5d26b95d1e980fc0746185234fc`, READY `dpl_HXtcLUYrTgGwMDCfgVJtWdpu2zgU` at the production alias. Evidence: `ops/deployments/vercel-production-2026-09-05-ranked-proof-browser.json`. HTTP 200 and exact CSP were verified on the HTML and compiled prover Worker. Ranked entry remains visibly fail-closed. Reload starts a fresh local playtest; real mining produces IS-4E7D4 plus one natural neighbor and 1,024 searched units². Same-build reload restores both Planets with increasing Home energy. No production browser errors were captured.
- **Rollback:** Previous deployment is available. Following the owner's policy, deploying a rollback build also starts a fresh local playtest. Never alter ranked vaults, wallet/Soul or chain data. No chain rollback is needed.
- **Current handoff:** `docs/codex/handoffs/2026-09-05-ranked-proof-browser.md`.
