# Infinite Stellar Current Phase

- **Status:** In progress (mainnet execution remains blocked until every production gate has real evidence)
- **Phase:** Source-backed interaction and portable private-map delivery
- **Goal:** Close every implementable production release gate, pin reproducible proof and audit evidence, prepare the exact multisig-controlled Sui publish/bootstrap bundle, and execute it only after independent audit clearance and the required real signers approve.
- **Authorization:** GitHub and Vercel delivery plus production-valid Sui mainnet engineering are authorized. Development keys, missing audits, unfinished services, or absent signer approvals cannot be substituted.
- **Outcome:** Release-gate closure audit started from source commit `2905327`. No production assertion or chain write is accepted without inspectable evidence.
- **Acceptance checks:** Reproducible production circuits and verifying-key digests are pinned; independent circuit/Move/SDK/client audits have no unresolved critical or high findings; multisig members, threshold, address, gas policy, capability custody, recovery, and signer verification are recorded; the exact publish/bootstrap transactions pass bytecode, dependency, budget, simulation, and two-wallet rehearsal gates; mainnet package/config/shared objects and Vercel configuration are verified from finality; deployment evidence is committed and public; no development key or self-review substitutes for an external gate.
- **Mainnet status:** No Infinite Stellar game transaction was signed or submitted. Production verifier readiness remains false.
- **Remaining blockers:** Under audit; known blockers are production proof ceremony, independent audits, signer identities/approvals, multisig/capability custody, public checkpoint indexer, sponsor/monitoring, two-wallet soak, Artifact/ship adapters, and operations/rights clearance.
- **Delivery plan:** `docs/18-delivery-and-season-operations.md` records the five owner-confirmed milestones and proposed 32-player/72-hour pilot followed by 64–128-player/seven-day seasons. Timing and capacity are proposals awaiting playtest evidence, not deployed rules.
- **Latest implemented increment:** Shared planet visuals, route previews, responsive panels and clear-view controls are pushed in `e8e5ad4`. Manifest-committed ranked mining now runs in a bounded cancellable Worker, revalidates discoveries before encrypted persistence, and refreshes deterministic chain point reads. Cross-tab vault writes preserve the union of discoveries. Conquered founding planets retain their provenance and show the chain's new owner. None of this opens ranked signing.
- **Verification:** Typecheck, lint, production build and 164 TypeScript tests passed on September 5, including the exploration UI callback. Source `8439b2a` is pushed and Vercel deployment `dpl_46KUx7ZEKdD8U4NWaqNMZzXA8t5u` is READY on `https://infinite-stellar.vercel.app`. HTTP/security headers and fail-closed mainnet browser route passed. Deployment evidence: `ops/deployments/vercel-production-2026-09-05-ranked-exploration.json`.
- **Owner priority, September 5:** Study DF source and experience the supplied INFINITY SPCX game before treating the current interaction model as correct. Continuous exploration, inspect-versus-send state, per-origin energy controls and in-map targeting feedback have confirmed source mismatches. Findings and acceptance checks: `docs/19-df-interaction-audit.md`. Reference browser has no saved account; the owner was asked for an already-entered session without sharing private keys.
- **Portable recovery:** The ranked client now exports namespace-bound authenticated portable backups, verifies restored preimages locally, obtains ownership/resources from fresh public point reads, and merges without replacing existing discoveries. Real Web Crypto, UI and hook tests cover tampering, wrong identity/password, failed saves/reads and late Seat changes. See `docs/20-private-map-backup.md`; no independent audit or live two-wallet recovery is claimed.
- **Expanded owner-requested review:** `docs/19-df-interaction-audit.md` now traces selected-Planet abilities through all fleet/ship/artifact/abandonment callbacks and settlement. Confirmed gaps include hard-coded artifact-send resources, mismatched action previews, hover versus dispatch target state, missing capability predicates, floor-versus-proof-bound distance, and missing public ranked action adapters. Original DF frontend Wormhole math also differs from its contract; use contract authority.
- **Continuous exploration (unpublished):** Local/ranked loops now search aligned 16-unit chunks continuously until paused, skip stored coverage, and support camera-center relocation and resume. Completed empty chunks persist; a shared map layer reveals exact footprints and marks active work/origin. Worker scope/hash and complete-batch validation remain enforced. A closed-season chain read stops ranked exploration and disables restart. Backup import/export must wait for exploration to be paused, including gaps between batches.
- **Unified-command correction (unpublished):** Fleet/cargo/ship/abandonment now use an explicit origin/target/resource intent and the same pure transition for preview and execution; abilities derive eligibility from selected-Planet validation. Exact ceiling route bounds, ship Wormholes, preservation of artifacts when claiming ships, blank/Escape deselection and compact aiming overlay fixes are implemented. Browser clicks verified 75% dispatch, neutral conquest, reinforcement, one-voyage drag-send and blank deselection. These are local-simulation results only.
- **Camera/controls:** Measured isotropic projection, cursor-anchored wheel zoom, stable manual zoom, local-world-centered panning, direct-space reach/cursor guidance, DF resource shortcuts and one-point controls are implemented. Move explorer is an explicit click-to-place mode. Compact aiming keeps keyboard focus and the toolbar avoids overlapping controls.
- **Current verification:** Typecheck, lint, production build and all 230 tests pass (102 web, 90 SDK, 38 prover); docs lint and diff checks pass. Browser checks cover 75% click/drag dispatch, conquest/reinforcement, refresh/resume exploration, 50→60% keyboard adjustment while aiming, Escape and click-to-relocate without a new fleet. This is a client increment, not a ranked multiplayer release. Source delivery and production-alias verification are next.
- **Exact next action:** Commit/push and deploy the verified client increment under existing authorization, record immutable deployment/rollback evidence and verify the live fail-closed route. Then continue real ranked proof preparation/special-action adapters, pending/finality and animated/partially known voyages. Observe the owner's authorized live reference session when available; preserve all audit/multisig gates.
- **Current handoff:** `docs/codex/handoffs/2026-09-05-unified-planet-commands.md`
